// ao iniciar a pagina
$(document).ready(iniciar);

function iniciar(){

    // evento ao clicar (click)
    // hide oculta
    $("div p").hide();
    $("span").click(function(){
        // show mostra
        $("div p").show();
    })

    
}